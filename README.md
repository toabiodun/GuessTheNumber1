# 🎯 Guess The Number (C# WinForms)

A simple Windows Forms game where you guess a secret number within a chosen range.

## Features
- Set **Min** and **Max** with sliders
- **Start** locks the range and generates a secret number
- **Guess** slider + button with color-coded hints:
  - 🔴 Higher
  - 🔵 Lower
  - 🟢 Correct (shows attempts)
- **New** resets the round

## How to Run
1. Open `GuessTheNumber.sln` in Visual Studio.
2. Set configuration to `Debug` or `Release`.
3. Build and run.

## Tech
- C# (.NET, WinForms)
- Visual Studio
